config:
  target: "http://localhost:3004"
  phases:
    - duration: 60  # Dura 60 segundos
      arrivalRate: 200  # 200 solicitudes por segundo
      rampTo: 500  # Aumenta hasta 500 solicitudes por segundo

scenarios:
  - flow:
      - post:
          url: "/order"